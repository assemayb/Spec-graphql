query listThreads($sortBy: String!) {
  listThreads(sortBy: $sortBy) {
    id
    question
    specialization
    threadCreator
    createdAt
    replies {
      id
      upvotes
      text
      replyThread
      replySpecialist
    }
  }
}
