query getThreadData($id: Int!) {
  getThread(id: $id) {
    question
    specialization
    threadCreator
    createdAt
    replies {
      id
      upvotes
      text
      replyThread
      replySpecialist
    }
  }
}
