
mutation addReply($text: String!, $replyThread: Int!, $replySpecialist: Int!) {
    addReply(options: {
        text: $text,
        replyThread: $replyThread,
        replySpecialist: $replySpecialist
    } )
}